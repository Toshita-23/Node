// const math = require('./math')

// We can also destructure the function here like below
const {add, sub} =  require('./math');

// console.log(math.add(2, 5));
// console.log(math.sub(2, 5));

// Directly call the functions here:
console.log(add(2, 5));
console.log(sub(2, 5));


console.log("Hello World");